# ── Core ──
fastapi==0.115.*
uvicorn[standard]==0.34.*
pydantic-settings==2.*

# ── Database ──
sqlalchemy[asyncio]==2.*
aiosqlite==0.20.*

# ── HTTP client ──
httpx==0.28.*
duckduckgo-search>=6.*

# ── Content extraction ──
trafilatura>=1.9.*

# ── Google Integration ──
google-auth>=2.*
google-auth-oauthlib>=1.*
google-api-python-client>=2.*

# ── Browser Agent ──
# patchright is a stealth-patched Playwright fork — same API, bypasses
# Cloudflare, Akamai, DataDome bot detection.  Falls back to playwright
# automatically if not installed.
patchright>=1.49.*
# Keep playwright as fallback (patchright is a drop-in replacement)
playwright>=1.49.*

# ── AI / Embeddings ──
numpy==2.*

# ── Voice ──
faster-whisper==1.*
edge-tts==7.*

# ── Scheduler (lightweight, no Redis needed) ──
# Built-in asyncio scheduler, no extra deps needed

# ── Logging ──
loguru>=0.7.*

# ── Logging ──
loguru>=0.7.*

# ── Dev ──
python-multipart==0.0.*
